<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>마이페이지</title>
</head>
<body>
    즐겨찾기 리스트<br>
    <table id="userFavoriteData">
        <% for(let i = 0; i < userFavoriteData.length; i++){ %>
            <tr>
                <td><%=userFavoriteData[i].char_name%></td>
                <td><%=userFavoriteData[i].comm_num %></td>
            </tr>
        <% } %> 
    </table>
</body>
</html>